import {
  CITY_IMAGES,
  CITY_PRICING_MULTIPLIERS,
  DISCOVERY_SPACES,
  LOCATIONS,
  TESTIMONIALS,
  WORKSPACES,
} from '@/data/index.js';

/** Catalog data used by the widgets (locations, workspaces, discovery spaces, testimonials, pricing rules). */
export const catalog = {
  locations: LOCATIONS,
  workspaces: WORKSPACES,
  discoverySpaces: DISCOVERY_SPACES,
  testimonials: TESTIMONIALS,
  cityImages: CITY_IMAGES,
  cityPricing: CITY_PRICING_MULTIPLIERS,
};
